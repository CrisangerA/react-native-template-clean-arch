import {StyleSheet} from 'react-native';

export const textStyles = StyleSheet.create({
  root: {},
  title: {
    fontSize: 28,
    fontWeight: 'bold',
    marginBottom: 8,
  },
  subtitle: {
    fontSize: 20,
  },
});
